# Database Configuration
POSTGRES_USER=pv3monitor
POSTGRES_PASSWORD=pv3monitor_dev_password
POSTGRES_DB=pv3monitor
DATABASE_URL=postgresql+asyncpg://pv3monitor:pv3monitor_dev_password@db:5432/pv3monitor

# MQTT Configuration
# For local P3 connection, use your P3's IP address
# For Docker development, use the mosquitto service name
MQTT_HOST=192.168.1.215
MQTT_PORT=1883

# P3 Device Configuration
# Replace with your actual P3 device ID (found in MQTT topics)
P3_DEVICE_ID=PV001001DEV

# API Configuration
API_HOST=0.0.0.0
API_PORT=8800

# Frontend Configuration
# Use server IP for browser access, or localhost if accessing from the server itself
VITE_API_URL=http://192.168.1.6:8800
VITE_WS_URL=ws://192.168.1.6:8800

